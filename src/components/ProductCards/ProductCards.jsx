import React, { use } from 'react';

const ProductCards = ({productCardsJson}) => {

    const productCardsData=use(productCardsJson);

   

    return (
        <div>
             
                
    
            
        </div>
    );
};

export default ProductCards;